{
  "name": "time-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:remote": "wrangler d1 migrations apply time-management-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
